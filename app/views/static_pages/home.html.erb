<div id="content" class="contaner">
  <div class="jumbotron">
    <div class="hello">
      <h2>It's like <em>Cards Against Humanity</em>. Except with Sketches</h2>
      <p>Create a sketch. Submit a card. Watch players try to match them.</p>
    </div>
    <p><a class="btn btn-lg" role="button">Learn more</a></p>
  </div> 
  <div class="sign-in-container">
   <input
      id="user_password_confirmation"
      name="user[password_confirmation]"
      type="hidden">

     <div>
   <input type="submit" value="Play!" class="btn new-user">
   <script>
   $("#new-user").click(function(){
    <%= render "devise/registration/new", email:  %>>
   })
   </script>
  </div>
  
  <div class="row">
    <div class="col-xs-6">
      <%= render "play" %> 
    </div>
    <div class="col-xs-6">
      <%= render "guest" %> 
    </div>
  </div> 
</div>

<script>
  <% if current_user  %>
  	  var currentUserID = <%= current_user.id %>;
  <% end %>
</script>



